/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ChefPipe } from './chef.pipe';

describe('ChefPipe', () => {
  it('create an instance', () => {
    let pipe = new ChefPipe();
    expect(pipe).toBeTruthy();
  });
});
